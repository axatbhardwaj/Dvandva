interface:
  display_name: "Dvandva Prativadi"
  short_description: "Review inside a paired Dvandva run"
  default_prompt: "Use $prativadi to join this exact paired Dvandva run."
policy:
  allow_implicit_invocation: true
