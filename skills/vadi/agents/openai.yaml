interface:
  display_name: "Dvandva Vadi"
  short_description: "Implement inside a paired Dvandva run"
  default_prompt: "Use $vadi to implement this ticket in a paired Dvandva run."
policy:
  allow_implicit_invocation: true
