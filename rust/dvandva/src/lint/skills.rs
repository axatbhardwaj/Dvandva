//! `lint skills` logic — Wave B port target.

/// Run the lint against `args`; returns the process exit code (0 pass, 1
/// findings, 2 usage).
pub fn run(args: &[String]) -> i32 {
    let _ = args;
    eprintln!("dvandva lint skills: not implemented");
    2
}
